# MyCycleCoach Docker Environment Configuration Example
# Copy this file to .env and customize for your environment
# For production, use GitHub Secrets or your container orchestration platform's secrets management

# Database Configuration
# The database will be created automatically as POSTGRES_DB during container startup
DB_NAME=mycyclecoach
DB_USERNAME=cyclecoach
DB_PASSWORD=localdev123

# Spring Profile
# Options: dev (default), test, prod
# Controls which application-{profile}.yaml is loaded
SPRING_PROFILES_ACTIVE=dev

# Kafka Configuration
# The bootstrap servers are set automatically in docker-compose.yml
# Override here only if using external Kafka cluster
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# JVM Configuration (optional)
# Uncomment and customize if needed for memory constraints or performance tuning
# JAVA_OPTS=-Xmx512m -Xms256m

# Logging Level (optional)
# Options: DEBUG, INFO, WARN, ERROR
# CONFIG_LOG_LEVEL=INFO

# ============================================================================
# PRODUCTION DEPLOYMENT CONFIGURATION
# ============================================================================
# The following variables are required for AWS EC2 deployment via GitHub Actions
# Set these as GitHub Secrets (Settings > Secrets and variables > Actions)

# AWS Configuration
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_REGION=us-east-1

# EC2 Instance Configuration
# EC2_HOST=ec2-xx-xx-xx-xx.compute-1.amazonaws.com
# EC2_USER=ec2-user
# EC2_SSH_KEY=-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----

# Database (Production - RDS or external PostgreSQL)
# DB_URL=jdbc:postgresql://your-rds-instance.region.rds.amazonaws.com:5432/mycyclecoach
# DB_USERNAME=your-db-username
# DB_PASSWORD=your-secure-db-password

# JWT Configuration (Production - use a strong secret)
# JWT_SECRET=your-very-long-and-secure-jwt-secret-at-least-256-bits

# Strava API Configuration
# STRAVA_CLIENT_ID=your-strava-client-id
# STRAVA_CLIENT_SECRET=your-strava-client-secret
# STRAVA_REDIRECT_URI=https://yourdomain.com/api/v1/strava/callback

# Email Configuration (Production SMTP)
# MAIL_HOST=smtp.gmail.com
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-specific-password
# EMAIL_FROM=noreply@mycyclecoach.com
# EMAIL_VERIFICATION_URL=https://yourdomain.com/api/v1/auth/verify
