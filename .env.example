# MyCycleCoach Docker Environment Configuration Example
# Copy this file to .env and customize for your environment
# For production, use GitHub Secrets or your container orchestration platform's secrets management

# Database Configuration
# The database will be created automatically as POSTGRES_DB during container startup
DB_NAME=mycyclecoach
DB_USERNAME=cyclecoach
DB_PASSWORD=localdev123

# Spring Profile
# Options: dev (default), test, prod
# Controls which application-{profile}.yaml is loaded
SPRING_PROFILES_ACTIVE=dev

# Kafka Configuration
# The bootstrap servers are set automatically in docker-compose.yml
# Override here only if using external Kafka cluster
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092

# JVM Configuration (optional)
# Uncomment and customize if needed for memory constraints or performance tuning
# JAVA_OPTS=-Xmx512m -Xms256m

# Logging Level (optional)
# Options: DEBUG, INFO, WARN, ERROR
# CONFIG_LOG_LEVEL=INFO
